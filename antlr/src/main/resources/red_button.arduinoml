application redButton

# Declaring bricks
sensor button: 9
actuator led: 12

# Declaring states
on {
    led <= HIGH
    elapsed time is 500 => off
}

-> off {
    led <= LOW
    button is HIGH elapsed time is 1000 => on
    elapsed time is 10000 => on
}
