application redButton

# Declaring bricks
sensor button: 9
actuator led: 12

# Declaring states
on {
    led <= HIGH
    elapsed time is 10 => off
}

-> off {
    led <= LOW
    button is HIGH => on
}
